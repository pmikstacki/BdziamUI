<BSwitch 
    Value="@IsOn"
    ValueChanged="OnSwitchToggle"
/>

@if (IsOn)
{
<BText Typo="Typo.Body" Color="MaterialColor.SuccessContainer"> I Wonder what this does... </BText>
}

@code {
    private bool IsOn { get; set; } = false;

    private void OnSwitchToggle(bool value)
    {
        IsOn = value;
        StateHasChanged();
    }
}